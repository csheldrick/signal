{
  "name": "signal-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Signal — a local-first collaborative knowledge workspace",
  "scripts": {
    "build": "tsc",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^25.9.1",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0"
  }
}
