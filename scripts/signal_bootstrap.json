[
  {
    "type": "goal",
    "description": "Provide robust real-time collaboration for documents via SignalApp, ensuring low-latency presence updates (PresenceTracker, PeerPresence) and consistent document state (DocumentStore, SyncEngine, SyncManager)."
  },
  {
    "type": "goal",
    "description": "Support extensible indexing and search across documents using InvertedIndex, GraphBuilder, and SearchPlugin so users can query documents with SearchQuery and receive SearchResult/SearchHit."
  },
  {
    "type": "goal",
    "description": "Offer AI-assisted document summarization with pluggable implementations: LocalSummarizer for fast local summaries and RemoteSummarizer for higher-quality remote summaries (Summarizer contract)."
  },
  {
    "type": "goal",
    "description": "Expose a PluginHost/PluginContext model so plugins (ExportPlugin, SearchPlugin) can safely access documents and storage events (StorageEventBus, StorageEvent types) without breaking core invariants."
  },
  {
    "type": "goal",
    "description": "Ensure operational quality: observability, persistent storage durability, async job processing (background summarization/sync), authentication and adequate test coverage as first-class requirements."
  },
  {
    "type": "architecture",
    "description": "Module boundary signal-app contains core runtime: app/* implements services DocumentStore, SyncEngine, SyncManager, ClockProvider and host-level SignalApp which composes these services."
  },
  {
    "type": "architecture",
    "description": "Contract-driven design: many domain interfaces (Summarizer, DocumentReader, PresenceStatus, InvertedIndex, PluginContext) define behaviors; implementations (LocalSummarizer, RemoteSummarizer, PresenceTracker) are wired in the app module."
  },
  {
    "type": "architecture",
    "description": "Event-driven storage model: StorageEventBus plus typed StorageEventCreated/Updated/Deleted/Linked form the observable boundary for indexing, plugin notifications, and sync propagation."
  },
  {
    "type": "architecture",
    "description": "Sync and conflict model: SyncEngine, SyncManager, VectorClock, ConflictCandidate, and ConflictResolution indicate a vector-clock-based synchronization design with explicit conflict resolution strategies."
  },
  {
    "type": "architecture",
    "description": "Plugin isolation: PluginHost and PluginContext are intended to give plugins controlled access to DocumentStore and runtime services (refactor note: Presence refactor uses PluginContext for document access)."
  },
  {
    "type": "observation",
    "description": "LocalSummarizer is implemented as a pure function (LocalSummarizer.summarize) in app/src/ai/summarizer.ts; RemoteSummarizer is asynchronous and has side effects — two distinct runtime profiles for the Summarizer contract."
  },
  {
    "type": "observation",
    "description": "PresenceTracker exposes join(peerId, documentId?) returning PeerPresence and has async side effects; leave() is marked pure in the analysis which may mask necessary cleanup I/O."
  },
  {
    "type": "observation",
    "description": "createValidatorFromStore(_store: DocumentStore) performs I/O and is used in presence validation; this ties presence logic directly to DocumentStore visibility/integrity."
  },
  {
    "type": "observation",
    "description": "There is an explicit refactor: '[REFACTOR] fix(presence): refactor to use PluginContext for document access and remove dire' — code changed to route document access through PluginContext but the note 'remove dire' suggests a removed primitive that other modules might still depend on."
  },
  {
    "type": "observation",
    "description": "Multiple service nodes are duplicated in the topology (DocumentStore, SyncEngine, ClockProvider, SyncManager appear more than once) — the runtime likely has multiple instances or name collisions in analysis."
  },
  {
    "type": "observation",
    "description": "StorageEvent types (Created, Updated, Deleted, Linked) and StorageEventBus exist but there's no explicit Contract: StorageEventBus implementation node surfaced — potential gap between contract and concrete event broker."
  },
  {
    "type": "observation",
    "description": "Indexing components (InvertedIndex, GraphBuilder, AdjacencyList, IndexStats) are present; GraphBuilder and inverted index roles are separate which implies offline or async indexing stages."
  },
  {
    "type": "observation",
    "description": "Decision nodes 'TypeScript adoption' and 'Testing adoption' exist — the project contains an explicit decision to adopt TypeScript and increase test coverage, but 'Test coverage required' is flagged as a constraint indicating coverage is not yet sufficient."
  },
  {
    "type": "observation",
    "description": "Top-level module signal-app contains ~20 source files; overall graph reports 187 nodes and 220 edges implying substantial unexposed or library code beyond the app module."
  },
  {
    "type": "constraint",
    "description": "Authentication required: all remote operations (RemoteSummarizer calls, SyncEngine transport) must validate credentials/authorization before accessing documents or performing sync."
  },
  {
    "type": "constraint",
    "description": "Persistent storage: DocumentStore must persist document versions and version history; SyncEngine/SyncManager rely on durable storage to compute VectorClock, VersionHistory, and ConflictResolution correctly."
  },
  {
    "type": "constraint",
    "description": "Async job processing: background tasks (summarization, indexing, conflict resolution) must be resilient, idempotent, and observable — failures should not cause data loss or inconsistent indices."
  },
  {
    "type": "constraint",
    "description": "Observability: StorageEventBus and SyncManager must emit sufficient events/metrics to reconstruct system state and diagnose sync/conflict incidents (IndexStats, StorageEvent traces)."
  },
  {
    "type": "constraint",
    "description": "Input validation boundary and guard clauses: domain invariants (Document, DocumentChange, SearchQuery) must be validated at module boundaries to avoid corrupting InvertedIndex or GraphBuilder state."
  },
  {
    "type": "hypothesis",
    "description": "RemoteSummarizer network failures or rate limits will cause visible delays in summarization pipelines; without retries and backpressure the summarization queue could backlog and affect SyncManager."
  },
  {
    "type": "hypothesis",
    "description": "PresenceTracker.join/leave race conditions will surface under concurrent peers: if leave() is implemented as a pure function (no I/O) but must update shared state, presence may leak stale PeerPresence entries."
  },
  {
    "type": "hypothesis",
    "description": "Index staleness: if StorageEventBus implementation is missing or not hooked into DocumentStore, InvertedIndex and GraphBuilder will be out of sync after DocumentStore updates, causing incorrect SearchResult sets."
  },
  {
    "type": "hypothesis",
    "description": "After the refactor to use PluginContext for document access, some plugins or consumers still referencing the removed 'dire' primitive will break at runtime causing plugin initialization failures in PluginHost."
  },
  {
    "type": "hypothesis",
    "description": "ConflictResolution quality will degrade if SyncEngine instances are duplicated or misconfigured (duplicate DocumentStore/SyncEngine nodes observed); redundant instances may create divergent VectorClock histories."
  },
  {
    "type": "goal",
    "description": "Implement offline-first document sync via an OfflineSyncQueue (app/src/sync/offline-queue.ts) that buffers DocumentChange mutations when the transport is unavailable, persists them durably, and drains them in causal order when the peer reconnects — ensuring no mutations are lost across network interruptions."
  },
  {
    "type": "goal",
    "description": "Provide a SyncSessionTracker (app/src/sync/session-tracker.ts) that manages per-peer sync session lifecycle: session open/close, heartbeat emission, stale-session detection via configurable timeout, and a SyncSessionEvent bus so SyncManager and PresenceTracker share a single authoritative view of active sessions."
  },
  {
    "type": "goal",
    "description": "Introduce a DocumentSnapshotService (app/src/storage/snapshot-service.ts) that periodically compacts document history into versioned snapshots, bounds VectorClock vector size, and exposes a SnapshotStore contract so SyncEngine can use the latest snapshot as a fast-forward base during conflict resolution."
  },
  {
    "type": "observation",
    "description": "No offline mutation buffer currently exists between DocumentStore and SyncEngine; if the transport drops mid-session, in-flight DocumentChange records are discarded and re-creation depends on the user repeating edits — a data-loss surface that OfflineSyncQueue would close."
  },
  {
    "type": "observation",
    "description": "VectorClock vectors grow unboundedly as peers join and leave; without periodic snapshot compaction the clock comparison cost in ConflictResolution grows O(n) with peer count, which will become a bottleneck in long-running collaborative sessions."
  },
  {
    "type": "observation",
    "description": "SyncManager has no session-level state today — it treats every sync attempt as stateless. A SyncSessionTracker would let it distinguish a reconnecting peer (needs delta replay from last known clock) from a first-time peer (needs full snapshot), eliminating redundant full-sync transfers."
  },
  {
    "type": "constraint",
    "description": "OfflineSyncQueue must persist buffered mutations to durable storage (same DocumentStore backend) with a monotonic sequence number so drain order is deterministic and idempotent on replay; duplicate application of a mutation must be a no-op, not a conflict."
  },
  {
    "type": "constraint",
    "description": "SyncSessionTracker must emit SyncSessionOpened, SyncSessionHeartbeat, SyncSessionStale, and SyncSessionClosed events on StorageEventBus so downstream consumers (PresenceTracker, SyncManager, observability layer) can react without polling."
  },
  {
    "type": "constraint",
    "description": "DocumentSnapshotService must write snapshots atomically (write-then-swap, never partial) and retain at least the two most recent snapshots so a rollback is always possible without a full history replay; snapshot creation must not block live document writes."
  },
  {
    "type": "hypothesis",
    "description": "Without OfflineSyncQueue, mobile or flaky-network clients will produce silent edit loss: SyncEngine will report success (local apply) but the mutation will never reach peers, causing invisible divergence that ConflictResolution cannot detect because the missing change was never transmitted."
  },
  {
    "type": "hypothesis",
    "description": "A DocumentSnapshotService that snapshots on a fixed wall-clock interval (e.g. every 5 minutes) will be insufficient under burst-edit workloads; snapshot frequency should be change-count-triggered (e.g. every N DocumentChange events) to keep VectorClock bounds tight regardless of edit rate."
  }
]